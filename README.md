> ⚠️ **Lưu ý:**
> 
> 1. Chạy lệnh sau để tải trọng số của model:
>    ```bash
>    hf download Qwen/Qwen3-VL-Embedding-2B --local-dir ./models/Qwen3-VL-Embedding-2B
>    ```
> 2. Tạo trước thư mục có tên `train`, sau đó kéo và đổi tên file `split_coop` tương ứng trước khi chạy scripts huấn luyện.
>    * **Ví dụ:** Khi chạy bộ dữ liệu `dtd`, tạo thư mục `train`, kéo file `split_coop_dtd.csv` vào đó và đổi tên thành `split_coop.csv`.
